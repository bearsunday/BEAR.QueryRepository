{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://bearsunday.github.io/BEAR.QueryRepository/schemas/context/cache_hit.json",
    "title": "cache_hit",
    "description": "Close/event: a cache lookup hit at the given layer. As a scope close it reports only the final layer's outcome; sibling events in the same scope may show the entry was rebuilt (e.g. refresh_donut) or that a content-layer miss occurred, so a hit close does not by itself prove the whole subtree was served unchanged. Layer `etag` is the conditional-request answer at the transfer boundary: a hit there is the 304 decision, made before any resource runs.",
    "type": "object",
    "required": [
        "layer",
        "durationMs"
    ],
    "properties": {
        "layer": {
            "type": "string",
            "enum": [
                "resource",
                "donut",
                "donut-view",
                "etag"
            ]
        },
        "durationMs": {
            "description": "wall time from the scope opening to this close, in milliseconds. What is included depends on the layer and the outcome: layer resource/donut-view -> hit = serving from the pool; miss = resource run, and the write it triggered only for the cold (lone) and write-failed cases; layer etag -> the ETag pool lookup only, the resource runs after a false return if at all. Null on a bare event: an inner lookup has no scope, so it has no duration to report and a zero would read as \"instant\". A measurement, not a contract: it moves with the machine, the pool and the payload.",
            "type": [
                "number",
                "null"
            ],
            "minimum": 0
        }
    },
    "additionalProperties": false
}
