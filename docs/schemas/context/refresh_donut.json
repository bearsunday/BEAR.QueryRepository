{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://bearsunday.github.io/BEAR.QueryRepository/schemas/context/refresh_donut.json",
    "title": "refresh_donut",
    "description": "Event: a donut page was re-rendered after the donut TEMPLATE lookup hit. The cached template is reused; what is rebuilt is the rendered view — each embedded placeholder is re-fetched (refreshing that inner resource's own cache entry) and the template is re-rendered. It marks a partial rebuild, not a full miss.",
    "type": "object",
    "required": [
        "uri"
    ],
    "properties": {
        "uri": {
            "type": "string"
        }
    },
    "additionalProperties": false
}
