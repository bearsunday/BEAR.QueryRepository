{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://bearsunday.github.io/BEAR.QueryRepository/schemas/context/put_donut.json",
    "title": "put_donut",
    "description": "Event: a donut was put with its TTLs.",
    "type": "object",
    "required": [
        "uri",
        "ttl",
        "sMaxAge"
    ],
    "properties": {
        "uri": {
            "type": "string"
        },
        "ttl": {
            "description": "seconds until expiry of the donut template entry (putStatic: the $ttl argument; putDonut: $donutTtl). Interceptor-driven puts log null (no explicit TTL).",
            "type": [
                "integer",
                "null"
            ],
            "minimum": 0
        },
        "sMaxAge": {
            "description": "CDN s-maxage in seconds (putStatic only: also used as the rendered view's TTL; putDonut and interceptor-driven puts log null)",
            "type": [
                "integer",
                "null"
            ],
            "minimum": 0
        }
    },
    "additionalProperties": false
}
